# SNS共有時のOGP(og:url / og:image)に使う本番公開URL。
# ローカル開発では、このファイルを .env としてコピーして使う(.env は gitignore 済み)。
# Cloudflare Pages 上のビルドでは設定不要(vite.config.ts が
# CF_PAGES_BRANCH / CF_PAGES_URL から自動判定する)。
VITE_SITE_URL=https://keisan-renshu.com
